naftiko: 1.0.0-alpha2
info:
  label: Azure Load Balancer REST API — Operations
  description: 'Azure Load Balancer REST API — Operations. 1 operations. Lead operation: Azure Load Balancer REST API List
    operations. Self-contained Naftiko capability covering one Microsoft Azure Load Balancer business surface.'
  tags:
  - Microsoft Azure Load Balancer
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_LOAD_BALANCER_API_KEY: MICROSOFT_AZURE_LOAD_BALANCER_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-load-balancer-operations
    baseUri: https://management.azure.com
    description: Azure Load Balancer REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Network-operations
      path: /providers/Microsoft.Network/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Load Balancer REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_LOAD_BALANCER_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-load-balancer-operations-rest
    port: 8080
    description: REST adapter for Azure Load Balancer REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-network/operations
      name: providers-microsoft-network-operations
      description: REST surface for providers-Microsoft.Network-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Load Balancer REST API List operations
        call: microsoft-azure-load-balancer-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-load-balancer-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Load Balancer REST API — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: azure-load-balancer-rest-api
      description: Azure Load Balancer REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-load-balancer-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
